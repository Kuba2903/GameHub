@model IEnumerable<GenreVm>

<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>Tools</th>
            <th>
                Genre
                <a href="@Url.Action("Index", new { sortOrder = ViewBag.GenreSortParm })">
                    <i class="bi bi-arrow-down-up"></i>
                </a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var genre in Model)
        {
            @if (!string.IsNullOrEmpty(genre.Genre_Name))
            {
                <tr>
                    <td>
                        <a href="@Url.Action("Edit", new { id = genre.Id })" class="btn btn-primary btn-sm" title="Edit">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a href="@Url.Action("Details", new { id = genre.Id })" class="btn btn-info btn-sm" title="Details">
                            <i class="bi bi-info-circle"></i>
                        </a>
                        <a href="@Url.Action("Delete", new { id = genre.Id })" class="btn btn-danger btn-sm" title="Delete">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                    <td>@genre.Genre_Name</td>
                </tr>
            }
        }
    </tbody>
</table>

<a href="@Url.Action("Create")" class="btn btn-primary btn-sm" title="Create">
    Create
</a>